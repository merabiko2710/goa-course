// 1-დან 100-მდე გამოიტანეთ ორის მიმატებით ყველა რიცხვი.
const name22 =100
for (let pos = 1;pos < name22; pos +=2){
    console.log(pos)
}
// 1-დან 100-მდე გამოიტანეთ ყველა რიცხვი, ოღონდ გვერდზე ტექსტი ჰქონდეს ლუწია თუ კენტი.

const name2 =100
for (let pose = 1;pose < name2; pose ++){
    if(pose % 2 === 0){
        console.log("ლუწი")}
    else{
        console.log("კენტი")
    }
    }